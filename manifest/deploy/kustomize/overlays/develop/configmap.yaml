apiVersion: v1
kind: ConfigMap
metadata:
  name: template-single-configmap
data:
  config.yaml: |
    server:
      address:     ":8080"
      openapiPath: "/api.json"
      swaggerPath: "/swagger"

    logger:
      level : "all"
